import React from 'react';
import PropTypes from 'prop-types';

const Events = props => {
    return (
        <div>
            
        </div>
    );
};

Events.propTypes = {
    
};

export default Events;